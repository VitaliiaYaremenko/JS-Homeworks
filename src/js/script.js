'use strict';

const userX = +prompt('Write your number: ');
const userY = +prompt('Write your number: ');
const userZ = +prompt('Write your number: ');

alert((userX + userY + userZ) / 3);


// const average = [userX, userY, userZ].reduce((a, b) => a + b, 0);
// const sum = average / [userX, userY, userZ].length;
// alert(sum);